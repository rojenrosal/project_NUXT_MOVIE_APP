{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.4.3",
    "nuxt-windicss": "^2.6.1"
  },
  "dependencies": {
    "@vueuse/nuxt": "^10.1.2",
    "dotenv": "^16.0.3"
  }
}
